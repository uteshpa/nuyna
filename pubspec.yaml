name: nuyna
description: "Creator's Privacy Toolkit - Complete offline video privacy protection app."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  riverpod: ^3.1.0
  flutter_riverpod: ^3.1.0
  get_it: ^9.2.0
  intl: ^0.20.2
  path_provider: ^2.1.0
  image_picker: ^1.1.2
  video_player: ^2.9.1
  gallery_saver_plus: ^3.0.5
  share_plus: ^9.0.0
  # Sprint 2: Data Layer dependencies
  google_mlkit_face_detection: ^0.13.1
  ffmpeg_kit_flutter_minimal: ^6.0.8
  dartz: ^0.10.1
  # Sprint 6: Image processing with EXIF removal
  image: ^4.3.0
  flutter_image_compress: ^2.3.0
  video_thumbnail: ^0.5.6

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  riverpod_generator: ^4.0.0+1
  build_runner: ^2.4.0

flutter:
  uses-material-design: true
